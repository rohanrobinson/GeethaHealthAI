Clickable WireFlow (MVP)

Login / Create Family → Family Roster
Family Roster → tap member → Member Profile
Member Profile → tabs: Conditions, Appointments, Medications
Family Roster → “+ Add Member” → Add Member Form → back to Family Roster

First 3 screens to design

    1) Login / Create Family   (***DONE***)
    Goal: get a user into a household quickly.
    Email + password
    “Create new family” option (family name)
    Primary CTA: “Continue”


    2) Family Roster (***DONE***)
    Goal: list all members and enable add.
    Header: Family name + settings
    Member cards: name, age, role
    CTA: “+ Add member”
    Tap member → Member Profile

    3) Member Profile (Overview + Tabs)
    Goal: single place to see health data.
    Member header: name, age, role
    Tab bar: Conditions / Appointments / Medications
    Each tab: empty-state + “Add” CTA (can be read‑only in v1)
